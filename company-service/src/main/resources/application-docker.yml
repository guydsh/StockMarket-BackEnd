eureka:
  instance:
    hostname: company-service
  client:
    serviceUrl:
      defaultZone: http://registry-service:8761/eureka/

spring:
  jpa:
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:mysql://mysql-service:3306/smc_company
    username: root
    password: just4mysql